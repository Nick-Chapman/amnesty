
honesty/hnes: compute frame hash fingerprint.

Sim control: pause on space? do control in effect?
Keys vs buttons.

Concrete types for Bit/Byte(v)/Addr; pp hex; conversion
Typed primitives (like zagain)

Speed up emulation by compilation from effect to (typed?) Code.

Reduce Vmem read by x8 for BG rendering. Regs to save tile bytes. And att bytes.
Sprite evaluation; test via OAM dump from honesty. pick 8 of 64 sprites per line.

Cleanup Vmem MM; same for read/write
6502 ops
